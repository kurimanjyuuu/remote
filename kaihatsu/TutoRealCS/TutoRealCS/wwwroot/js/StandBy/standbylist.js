$(function () {
//    $('.StartDateCheck').on('change', function () {
//        // コントローラ名とアクション名を変数に格納
//        $('#Condition_StartDate').prop('disabled', !$(this).is(':checked'));
//    });
//    $('.EndDateCheck').on('change', function () {
//        // コントローラ名とアクション名を変数に格納
//        $('#Condition_EndDate').prop('disabled', !$(this).is(':checked'));
//    });
});